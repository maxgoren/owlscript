def fib(var n) {
    if (n < 2) {
        return 1;
    }
    return fib(n-1)+fib(n-2);
};
def main() {
    let i := 1;
    while (i <= 30) {
        println fib(i);
        i := i + 1;
    }
    return 0;
}