def takeone(var x) {
    if (x < 10) {
        println "its less than ten!";
    } else {
        println "never mind.";
    }
}

def taketwo(var x) {
    if (x < 10) {
        println "what am i doing here";
        println "and the days go by";
        return 420;
    }
    println "beats me...";
}

def fib(var x) {
    if (x < 2) {
        return 1;
    }
    return fib(x-1)+fib(x-2);
}

def main() {
    println "hi";
    takeone(13);
    println "oh okay";
    println taketwo(6);
    println map([5 .. 13], fib);
}