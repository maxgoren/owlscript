fn map(let xs, let f) { 
    let res := [];
    for (t of xs) {
        res.append(f(t));
    }
    return res;
}

fn filter(let xs, let f) {
    let res := [];
    for (t of xs) {
        if (f(t)) {
            res.append(t);
        }
    }
    return res;
}