import redblacktree;

let root := nil;
let sed := "asearchingexample";
let i := 0;
while (i < 17) {
    root := rbput(root, sed[i]);
    i++;
}
rbpreorder(root);
println " ";
rbinorder(root);
println " "
println isRedBlack(root);
